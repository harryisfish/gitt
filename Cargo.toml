[package]
name = "gitt"
version = "2.0.0"
edition = "2024"
description = "A lightweight TUI git status monitor for terminal split panes"
license = "MIT"

[dependencies]
ratatui = "0.29"
crossterm = { version = "0.28", features = ["event-stream"] }
git2 = "0.20"
anyhow = "1"
